import { useRef } from "react";

export default function SliderContainer(props) {
    return <div className="slider-container-title">{props.videoModel.title}</div>;
    // return <div className="slider-container-title">{props}</div>;
}